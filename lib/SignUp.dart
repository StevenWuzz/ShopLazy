import 'package:flutter/material.dart';

class SignUp extends StatelessWidget {
  @override //TODO Start and Finish
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('MainContent!'),
      ),
      body: Text("Helloooo World!"),
    );
  }
}
